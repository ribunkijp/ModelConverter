### CMake

```c++
cmake --build --preset x64-Debug --target clean
cmake --build --preset x64-Debug
或
Remove-Item -Recurse -Force build/x64-Debug
cmake --preset x64-Debug   
cmake --build --preset x64-Debug

```

